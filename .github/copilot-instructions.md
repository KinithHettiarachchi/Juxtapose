# Copilot Instructions

## Project Guidelines
- User wants a full refactor: replace WinForms UI with WebView2 hosting an HTML/CSS/JS UI that exactly mirrors the original two-tab layout and control placement, while preserving all existing functionality (SVN, comparison, export, backup/restore).